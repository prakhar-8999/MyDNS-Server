{
  "name": "mydns",
  "version": "1.0.0",
  "description": "creating simple dns server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dns-packet": "^5.6.1"
  }
}
